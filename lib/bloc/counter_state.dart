//This is the counter state class that stores the value of the counter
class CounterState {
  //a int variable is declared which ensures we only get integers
  final int count;

//constructor used to declare the value of the count variable
  CounterState({required this.count});
}
